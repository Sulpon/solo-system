import { createDefaultDashboardGridLayout, createDefaultDashboardLayout } from "../widgets/widget-registry";

export const dashboardLayout = createDefaultDashboardLayout();

export const dashboardGridLayout = createDefaultDashboardGridLayout();
